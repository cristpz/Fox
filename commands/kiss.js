const Discord = require("discord.js");
const superagent = require("superagent");

module.exports.run = async (bot, message, args) => {
  if(!message.mentions.members.first()) return message.reply(`Usage: !kiss <user>`);
 let result = Math.floor(Math.random() * 20); 
  
  if (result == 0) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 2) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 4) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 5) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 6) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 7) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 8) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 9) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 10) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 11) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 12) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 13) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 14) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 15) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 16) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 17) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 18) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 19) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  
  if (result == 20) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 3) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage(body.url);

  message.channel.send(kissembed);
  } 
  if (result == 1) {
    let {body} = await superagent
  .get(`https://nekos.life/api/v2/img/kiss`);

  let kissembed = new Discord.RichEmbed()
  .setColor("#ffa500")
  .setTitle(`${message.author.username} kissed ${message.mentions.members.first().user.username}`)
  .setTimestamp()
  .setFooter("Fox, a bot by Akemi#2314", bot.user.displayAvatarURL)
  .setImage("http://0x0.st/z4Kb.gif");

  message.channel.send(kissembed);
  } 
}

module.exports.help = {
  name: "kiss"
}
// http://0x0.st/z4Kb.gif